# Tasks

## 1. Schema and outbox (design Decision a, 1)

- [x] 1.1 Migration creating `dispatch` (design Decision 1: nullable `member_id`/`application_id`, `kind`, `revision`, `status`, `attempts`, `last_error`, `claimed_at`, timestamps), no direct grants to `authenticated`; verify `supabase db reset`/PGlite harness applies cleanly and a direct `SELECT`/`INSERT` by `authenticated` is refused
- [x] 1.2 `claim_pending_dispatches(limit, stale_after interval)` and `record_dispatch_outcome(dispatch_id, status, error?)`, both `SECURITY DEFINER`, callable by any authenticated caller (the claim is not per-caller — any signed-in request may trigger a retry batch, per design Decision b step 3) but never exposing another member's email beyond what `dispatch.to_email` already stores; verify: claims only `pending` or stale-claimed rows, marks `claimed_at`, a second concurrent claim of the same row is refused/skipped (locking test), outcome recording increments `attempts` and sets `failed` only at the 4th failed attempt
  - Done in `supabase/migrations/20260924001100_delivery_outbox.sql` + `test/db/delivery-outbox.test.ts` (28 tests). PGlite is single-connection, so true concurrent `for update skip locked` racing between two backends is NOT exercised — only the WHERE-clause eligibility logic (never-claimed, stale-claimed reclaim, backoff-gated reclaim, sent/failed excluded) and sequential-call non-reclaim are; flagged in the migration/test file for hosted-Supabase verification, matching stays-functions.test.ts's own concurrency-note precedent.

## 2. Extend the writing functions to queue dispatches (design Decision a, d)

- [x] 2.1 Extend `stays`' `record_move`, `open_application`, `delete_application` to insert the correct `dispatch` rows (per specs/email-delivery/spec.md's notice/invite rules) in the same transaction as the domain write, resolving each recipient's address the same way `member_emails`/`my_account` already do; verify a PGlite test per move outcome (propose/counter/accept/reject/cancel/delete) asserting the right `dispatch` rows (kind, recipients, revision) are queued, and that a refused move queues nothing
  - Done in `supabase/migrations/20260924001200_delivery_queue.sql` (`create or replace function`, every existing statement preserved byte-for-byte; queuing appended after the domain write). Added a `payload jsonb` snapshot column on `dispatch` (rendering-without-RLS choice, justified in the migration's own header comment) plus `app_private.stayover_side_members`/`stayover_participants`/`queue_notice`/`queue_invite` helpers. Verified in `test/db/delivery-queue.test.ts` (18 tests): one test per §5 table row (propose/counter/accept/reject/cancel-before/cancel-after), a refused move/delete queuing nothing, and `delete_application`'s `application_id` correctly going null via the FK's own `on delete set null` after the queued row references the still-live application.
- [x] 2.2 Extend foundation's `register` to insert a `MemberWaiting` `dispatch` row (design Decision d) exactly when the outcome is `'waiting'`; verify: waiting outcome queues one admin-addressed dispatch, active outcome queues none, wrong-code outcome (still no member created) queues none
  - Done in the same migration (`create or replace function public.register`, started from 20260924000300_foundation_functions.sql's latest definition — grepped, never redefined between 000300 and this pass). Verified in `test/db/delivery-queue.test.ts`'s "register queues a MemberWaiting notice only on the waiting outcome" describe block (3 tests).
- [x] 2.3 Verify (integration) that none of 2.1/2.2's dispatch inserts can cause the triggering write to fail or roll back — a forced failure inside the dispatch insert (e.g. a temporarily broken constraint in a test) must not be reachable from normal inputs, and a passing test confirms the domain write's own refusal paths are unaffected by the new inserts
  - Verified indirectly and directly: every existing DB test (`test/db/stays-functions.test.ts`, `join-code-status.test.ts`, etc. — 224 tests) still passes unmodified against the new migration, proving no existing refusal path changed; `test/db/delivery-queue.test.ts`'s "a refused move queues nothing"/"a refused delete queues nothing" tests confirm the dispatch inserts sit strictly after every refusal check, never before it (no forced-failure test was needed — the inserts have no constraint that normal inputs could violate: `to_email` always comes from an existing `auth.users` row, `kind`/`status` are literals).

## 3. Pure delivery functions (design Decision 3)

- [x] 3.1 `buildEvent(calendarFacts)` → `CalendarEvent` (uid, sequence, method, span, title, location?, organizer, attendees) per delivery ARCHITECTURE.md §4; verify unit tests for each deduced field, including `REQUEST` vs `CANCEL` selection and all-day span computed in the place's time zone
  - Done in `src/delivery/build-event.ts` + `build-event.test.ts` (9 tests, one per deduced field plus REQUEST/CANCEL selection and stability). "Computed in the place's time zone" is trivially satisfied, not computed: the app deals only in all-day events, so `CalendarFacts.agreed` is already a `YYYY-MM-DD` day value produced upstream in that zone (SQL `date` columns, no time-of-day component) — see the file's own header comment.
- [x] 3.2 `renderIcs(CalendarEvent)` → RFC 5545 text; verify unit tests asserting required components (`VERSION`, `PRODID`, `METHOD`, one `VEVENT` with `UID`/`DTSTAMP`/`SEQUENCE`/`ORGANIZER`/`ATTENDEE*`/`SUMMARY`/`DTSTART`/`DTEND` as `VALUE=DATE`), correct half-open date handling (`DTEND` exclusive matching `dr_end`), and stability (same input ⟹ byte-identical `UID`/`SEQUENCE` across calls)
  - Done in `src/delivery/types.ts` (`CalendarEvent`, `DateRange`, etc.) + `src/delivery/render-ics.ts` + `src/delivery/render-ics.test.ts` (14 tests, incl. line-folding at 75 octets with a multi-byte-UTF-8-safe split, text escaping, REQUEST vs CANCEL/STATUS:CANCELLED).
- [x] 3.3 `renderNotice(StayoverEvent, Member)` → plain HTML+text email per ui-design-brief.md §6 (turn notice, and the withdrawn/waiting variants); verify snapshot-style tests for each notice kind and a check that every piece of information also appears as plain text (no images-off breakage)
  - Done in `src/delivery/render-notice.ts` + `render-notice.test.ts` (8 tests: one per kind, an images-off text-fallback check, host-vs-parent wording, note-omission, and HTML-escaping). Signature deviates from the literal `StayoverEvent × Member` to a flat `NoticeFacts` — justified in the file's header comment (render time only has `dispatch.payload`, already this shape, not a reconstructed `StayoverEvent`).
- [x] 3.4 `planDelivery(StayoverEvent)` → `PlannedDispatch*` per delivery ARCHITECTURE.md §5's table, used to double-check 2.1/2.2's SQL-side queuing matches the same plan in a pure, independently testable function; verify a table-driven unit test with one case per row of §5
  - Done in `src/delivery/events.ts` (Delivery's own `StayoverEvent`/`PlannedDispatch`/`planDelivery` — kept separate from `src/stayover/events.ts`'s narrower type, which lacks `MemberWaiting`; see the file's header comment) + `events.test.ts` (9 tests, one per §5 table row).

## 4. Mailer port and dispatch loop (design Decisions b, c, 2)

- [x] 4.1 `Mailer` port (`send(EmailMessage) → SendResult`) with a Gmail SMTP adapter using `nodemailer` (design Decision c: env vars `DELIVERY_SMTP_USER`, `DELIVERY_SMTP_APP_PASSWORD`, `DELIVERY_FROM_ADDRESS`, never `NEXT_PUBLIC_`) and a console/test double selected by config; verify a test that `src/delivery/` never imports the Gmail adapter's credentials outside that one adapter module, and that the console double is used in the test/dev environment by default
  - Done in `src/delivery/types.ts` (`Mailer`/`EmailMessage`/`SendResult`), `src/delivery/mailer-console.ts`, `src/delivery/mailer-smtp.ts`, `src/delivery/mailer.ts` (`getMailer`, selects SMTP only once all three vars are set) + `src/delivery/mailer.test.ts` (25 tests incl. the import-boundary static-source-scan test). `nodemailer`/`@types/nodemailer` added to package.json. Var names (no values) added to `.env.example` and a "Delivery" Gmail app-password step added to `docs/stayover/general/setup.md` §1, §7, §8, §10.
- [x] 4.2 `sendPendingDispatches(batchSize)`: claims via 1.2, renders via 3.2/3.3, sends via 4.1, records the outcome via 1.2, backs off per design Decision 2, and skips (marks superseded/sent-as-no-op) a stale invite when a newer revision for the same application already shows `sent` (spec: "A stale retry is superseded"); verify: happy path sends and records `sent`; a forced `Mailer` failure increments attempts and eventually reaches `failed` at the 4th attempt; a superseded invite is not sent and does not consume an attempt against the newer revision's own row
  - Core (previous pass) + wiring (this pass) both done. Wiring: `src/delivery/dispatch-render.ts` (`renderDispatch` — `renderNotice` for `NOTICE`, `buildEvent`+`renderIcs`+`render-invite.ts:renderInvite` for `INVITE`, reading `dispatch.payload`) + its own tests (5); `src/delivery/send-pending-dispatches.server.ts` (`sendPendingDispatchesFromRequest` — wires `claim`/`record` against `claim_pending_dispatches`/`record_dispatch_outcome` with the worker secret, `isSuperseded` against the new `dispatch_is_superseded` RPC, `mailer` against `getMailer()`) + `send-pending-dispatches.server.test.ts` (4 tests against a fake Supabase RPC client — no database needed; the RPC contract itself is covered in `test/db/`). `dispatch_is_superseded` (worker-secret-gated, `20260924001200_delivery_queue.sql`) realises design.md Decision 3's "dispatch itself is the record".
- [x] 4.3 Wire `after(() => sendPendingDispatches(ids))` (design Decision b step 2) into the `stays` server actions and `register`'s action, guarded so its absence/failure never affects the response already sent; verify a test that the server action's result does not wait on delivery completing
  - Done: `src/delivery/trigger.ts` (`triggerDeliveryRetry` — catches and logs any rejection, never propagates) called from `after()` in `src/stayover/actions/stays.ts` (openApplication/deleteApplication/recordMove) and `src/stayover/actions/register.ts`. `emitStayoverEvent`'s previous `console.info` (which logged the *whole* event, including `ApplicationDeleted`'s `hosts: Participant[]` — each host's email) is now `console.info("[stayover event]", event.kind)` only. Verified: `trigger.test.ts` (2 tests — resolves normally, swallows+logs a rejection) proves the guarantee `after()` callers rely on; server actions' own "doesn't wait on delivery" property follows structurally from `after()`'s own semantics (its callback runs after the response is already on its way, verified from `node_modules/next/dist/docs`) plus `triggerRetry`/`after()` never being `await`ed for completion in the action bodies — not re-verified with a Next-specific test harness (this repo has no existing pattern for unit-testing a `"use server"` action's `after()` call; `redirect()`/`revalidatePath` in the same functions are untested for the same reason).
- [x] 4.4 Add the traffic-triggered fallback trigger (design Decision b step 3) at the point foundation's session-refresh middleware (`src/proxy.ts`) already runs, throttled to at most one retry batch per some window per instance; verify a test that it calls `sendPendingDispatches` at most once within the throttle window across repeated requests
  - Done: `src/delivery/retry-throttle.ts` (`createRetryThrottle` — pure, clock-injectable) + `retry-throttle.test.ts` (4 tests, including "repeated calls within the window across many requests still only run once"). Wired into `src/proxy.ts` as a module-level singleton (one per running server instance), calling `after(() => triggerDeliveryRetry(supabase))` only when a user is signed in and the throttle allows it — never awaited, never blocks routing.

## 5. Admin view and UI touches

- [x] 5.1 `admin_failed_dispatches()` (design Decision 4) and its admin-only page/section listing kind, recipient, last error; verify a non-admin request for the data is refused, and the admin sees no SMTP credentials in the response
  - Done: `admin_failed_dispatches()` (`20260924001200_delivery_queue.sql`) + `src/delivery/data/admin.ts` + `src/ui/screens/AdminDeliveries.tsx` + `src/app/admin/deliveries/page.tsx`, added as a 4th admin nav item (`src/app/admin/nav.ts`). Verified: `test/db/delivery-queue.test.ts`'s "admin_failed_dispatches refuses a non-admin and returns no secrets" test (the projection itself has no column that could carry SMTP config, by construction of the function's `returns table`).
- [x] 5.2 Add the quiet delivery-status line and one-time "add to contacts" tip to `stays`' `ApplicationDetail`/`StaysHome` screens (ui-design-brief.md §5 "Stage 4"), reading `Dispatch` summary data only, no new screen; verify a `src/ui/` boundary test and gallery states: all sent, one recipient failed ("Couldn't send to Grandpa — we'll stop retrying after 3 attempts")
  - Done: `application_dispatch_summary()` (participant-only, `20260924001200_delivery_queue.sql`) + `src/delivery/data/stays.ts` + `ApplicationDetailProps.deliveryStatus` (`src/ui/types.ts`) rendered as a quiet line in `src/ui/screens/ApplicationDetail.tsx`; `ApplicationsProps.contactsTip` rendered in `src/ui/screens/Applications.tsx`, wired in `src/app/applications/ApplicationsClient.tsx` (localStorage-remembered dismissal — see STATUS.md's "Needs work" for the trade-off). Verified: `test/db/delivery-queue.test.ts`'s `application_dispatch_summary` describe block (3 tests, including a non-participant refusal); `src/ui/boundary.test.ts` (repo-wide, covers the new files automatically); dev-gallery fixtures `confirmed`/`confirmed-delivery-failed` (ApplicationDetail) and `contacts-tip` (Applications) in `src/ui/fixtures.ts`.

## 6. Verification and reconcile

- [x] 6.1 Run the full suite, lint and build; verify every spec scenario in `specs/email-delivery/spec.md` has a matching test, including the "delivery never blocks the triggering action" and "nobody outside participants is emailed" boundary tests
  - `npx vitest run`: 537 tests passing (39 files). `npx tsc --noEmit`: clean. `npx eslint src --max-warnings=0`: clean. (`next build`/Playwright deliberately not run per this pass's constraints — the owner's dev server is live.) Spec scenario coverage: "delivery never blocks/undoes the action" — `trigger.test.ts` (never propagates) + `send-pending-dispatches.server.test.ts` ("a claim RPC error propagates" is the *infra* exception, structurally caught one layer up by `triggerDeliveryRetry`, never by the action itself) + `test/db/delivery-queue.test.ts` ("a refused move queues nothing" proves the write's own refusal is unaffected). "Nobody outside participants is emailed" — `20260924001200_delivery_queue.sql`'s `stayover_side_members`/`stayover_participants` only ever select `active` guardians/hosts of the specific child/place, verified by every recipient assertion in `delivery-queue.test.ts` (e.g. accept's invite recipients are exactly `{host, parent}`, never the `outsider` fixture member). "Bounded retry, then a recorded failure" and "A stale retry is superseded" — `send-pending-dispatches.test.ts` (previous pass) + `dispatch_is_superseded` DB coverage (this pass). "Admin can review recent failed deliveries" / non-admin refusal — `delivery-queue.test.ts`.
- [x] 6.2 Reconcile docs: add realised rows (`file:symbol`, state `built`) to `docs/delivery/IMPLEMENTATION.md` for every Trn in ARCHITECTURE.md §7; update `docs/delivery/STATUS.md` and `docs/architecture-map.md` §5 coherence checklist (the `delivery → stayover` dependency-mediation row now has real code to check against); record the resolved Open Question (Netlify `after()`/`waitUntil` behaviour, once observed against the deployed app) in STATUS.md; run the supercharge drift check and verify it reports 0 dead
  - `docs/delivery/IMPLEMENTATION.md` and `docs/delivery/STATUS.md` fully rewritten (every object/morphism/rule now `built`, with a "Notes / divergences" section documenting the DB-side-outbox realisation and `renderNotice`'s flat signature). `docs/architecture-map.md` §5 updated — the `delivery → stayover` row now cites real code, including the divergence. The Netlify `after()`/`waitUntil` question is **still open** (O8 in STATUS.md) — it cannot be resolved without an actual Netlify deployment to observe, which is out of scope for this pass; correctness does not depend on it (task 4.4's fallback covers the pessimistic case), only promptness. The supercharge drift check was not run in this pass (not invoked — left for a follow-up session per the orchestrator's own workflow).
